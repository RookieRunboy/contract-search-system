## 1. 后端实现
- [x] 1.1 更新 `backend/contractApi.py` 中的 `_extract_codes_from_filename` 函数，改为扫描所有片段。
- [x] 1.2 更新 `backend/update_existing_codes.py` 中的 `_extract_codes_from_filename` 函数以保持逻辑一致。
- [x] 1.3 使用 `backend/test_extraction.py`（或类似测试脚本）配合复杂文件名验证提取结果。
