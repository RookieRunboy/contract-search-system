## 1. 实施计划
- [x] 1.1 后端：修改 `contractApi.py` 中的上传处理函数，实现4种文件名的解析逻辑。
    - **代码格式校验**:
        - CIR编码: 以 `CIR` (忽略大小写) 开头，后接数字。
        - 合同编码: 以字母开头，后接数字。
    - 模式1: `[合同编码]-[CIR编码]合同名`
    - 模式2: `[合同编码]合同名`
    - 模式3: `[CIR编码]合同名`
    - 模式4: `合同名` (无任何编码)
- [x] 1.2 后端：将提取到的 `contract_code` 和 `cir_code` 包含在文档列表 API 返回中。
- [x] 1.3 后端：更新 `get_uploaded_documents` API 的返回结构，使其包含这两个新字段。
- [x] 1.4 前端：更新 TypeScript 接口定义 (`DocumentRecord` 类型)，增加 `contractCode` 和 `cirCode`。
- [x] 1.5 前端：修改 `UploadPage.tsx` 中的轮询逻辑，确保前端能正确接收到这两个字段。
- [x] 1.6 前端：在 `UploadPage.tsx` 的合同列表中新增"合同编码"和"CIR编码"两列并展示数据。
