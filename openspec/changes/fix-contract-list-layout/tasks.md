# Tasks: 修复合同列表排版问题

## 1. 前端实现
- [x] 1.1 修改 `UploadPage.tsx` 中表格列宽度配置
  - 为"合同名称"列添加 `minWidth: 200`，确保最小可读宽度
  - 压缩"合同编码"列宽度：150 → 130
  - 压缩"合同注册编码"列宽度：160 → 140
  - 压缩"上传时间"列宽度：160 → 140
  - 压缩"状态"列宽度：160 → 130
  - 压缩"操作"列宽度：160 → 140

- [x] 1.2 为 Table 组件添加横向滚动支持
  - 添加 `scroll={{ x: 1100 }}` 配置
  - 当屏幕宽度小于表格最小宽度时自动启用水平滚动

## 2. 验证
- [x] 2.1 在笔记本屏幕上验证合同名称列不再被过度压缩
- [x] 2.2 在大屏幕上验证表格布局正常，无多余水平滚动条
- [x] 2.3 验证合同名称可正常换行显示完整内容
